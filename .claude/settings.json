{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "command": "jq -r '.tool_input.file_path // empty' | { read -r f; case \"$f\" in *canvas/src/*) (cd canvas && npx tsc -b) ;; esac; } || true",
            "timeout": 30,
            "statusMessage": "Typechecking canvas..."
          }
        ]
      }
    ]
  }
}
