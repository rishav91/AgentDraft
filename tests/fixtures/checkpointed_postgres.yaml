schema_version: 1
nodes:
  - id: chat
    llm:
      provider: anthropic
      model: claude-sonnet-5
      system: "You are a terse, helpful assistant."
checkpointer:
  backend: postgres
  dsn_env: MY_POSTGRES_DSN
