schema_version: 1
nodes:
  - id: chat
    llm:
      provider: anthropic
      model: claude-sonnet-5
      system: "Use the echo tool when asked to echo something."
    tools:
      - tests.support.tools:echo
